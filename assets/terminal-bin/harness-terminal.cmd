@ECHO OFF
TITLE DeepSeek Harness Terminal
ECHO DeepSeek Harness terminal - runtime %DSH_RUNTIME_VERSION%
ECHO Working directory: %CD%
ECHO.
ECHO Recommended plugin command:
ECHO   dsh plugin --profile web add ^<package^>
ECHO.
ECHO Available commands: node, npm, npx, pnpm, dsh, and git when installed on Windows.
ECHO The first pnpm command may download the pinned pnpm version from the npm registry.
ECHO.
