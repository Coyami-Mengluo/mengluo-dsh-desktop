# Documentation screenshots

These screenshots were supplied by the maintainer for publication. They show the Harness start screen inside the desktop shell, Harness settings, and the community plugin store. Plugin listings reflect the capture, not a current service guarantee or a recommendation of the listed plugin.

Before adding or replacing a screenshot, inspect both its visible content and PNG metadata. Do not include credentials, private paths, conversations, private plugin configuration or private repository names. The source preflight accepts only the exact reviewed images and SHA-256 digests listed in [documentation-assets.mjs](../../scripts/documentation-assets.mjs). These supplied images were checked and copied without editing their pixels; no metadata chunks were present.

The application illustration visible inside the settings page retains the separate terms in [the artwork notice](../../assets/ARTWORK.md). The official Harness UI and DeepSeek branding shown in the desktop window remain the property of their respective rights holders and do not imply endorsement of this unofficial client.

这些图片由维护者提供并指定公开使用，展示桌面窗口内的 Harness 起始界面、Harness 设置和社区插件商店。插件列表仅代表拍摄时的状态，不保证当前服务情况，也不表示推荐列表中的插件。

更换图片前必须检查画面与 PNG 元数据，不得包含凭据、私人路径、会话、私密插件配置或私人仓库名。源码检查仅接受已列入摘要白名单的具体图片；这三张原图已检查并直接复制，未修改像素，不含元数据块。应用插画继续适用独立的图标素材说明；窗口内的官方 Harness 界面与 DeepSeek 标识不表示官方对本非官方客户端的背书。
